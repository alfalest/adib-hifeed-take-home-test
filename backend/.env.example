# SQLite (default - no setup required)
DATABASE_URL="file:./dev.db"

# PostgreSQL (alternative - requires Docker or local PostgreSQL)
# DATABASE_URL="postgresql://hifeed:hifeed_secret@localhost:5432/hifeed_db?schema=public"

PORT=3001
